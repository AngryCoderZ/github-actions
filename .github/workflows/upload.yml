name: React Project
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      # - name: Install dependencies
      #   run: npm ci
      # - name: building
      #   run: npm run build
       - name: test
         run: echo "working"